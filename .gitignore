.venv/
.claude/
.env
__pycache__/
*.pyc
.streamlit/secrets.toml
# Local persistent Chroma store — regenerated by `python -m rag.embeddings`;
# only relevant when CHROMA_HOST is unset (the Docker stack uses its own volume).
chroma_db/